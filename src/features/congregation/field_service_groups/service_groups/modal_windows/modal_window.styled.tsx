import { Box, styled } from '@mui/material';

export const StyledModalWindowConatiner = styled(Box)({
  padding: '24px',
  gap: '24px',
  borderRadius: 'var(--radius-xl)',
  maxWidth: '500px',
  display: 'flex',
  flexDirection: 'column',
  backgroundColor: 'var(--white)',
});
