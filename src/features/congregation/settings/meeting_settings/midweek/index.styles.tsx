import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const MidweekContainer = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  gap: '16px',
}) as unknown as typeof Box;

export const LinkedPartsSection = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  gap: '16px',
  marginTop: '8px',
}) as unknown as typeof Box;
