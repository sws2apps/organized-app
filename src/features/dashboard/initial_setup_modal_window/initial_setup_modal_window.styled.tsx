import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const Gapped16Box = styled(Box)({
  display: 'flex',
  gap: '16px',
});

export const Gapped4Column = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  gap: '4px',
});
