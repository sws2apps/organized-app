import Typography from '@mui/material/Typography';

const S1Header = ({ header }) => {
  return <Typography sx={{ fontWeight: 'bold', fontSize: '20px' }}>{header}</Typography>;
};

export default S1Header;
