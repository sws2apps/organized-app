import { useState } from 'react';

const useScheduleAutofill = () => {
  const [startWeekOptions, setStartWeekOptions] = useState([]);
  const [endWeekOptions, endStartWeekOptions] = useState([]);

  return {};
};

export default useScheduleAutofill;
