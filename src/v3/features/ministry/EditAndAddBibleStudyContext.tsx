import { createContext } from 'react';

/**
 * Context for managing the state related to editing and adding Bible studies.
 * This context provides state and functions to update the state for managing pop-up windows and item data.
 */
export const EditAndAddBibleStudyContext = createContext(null);
